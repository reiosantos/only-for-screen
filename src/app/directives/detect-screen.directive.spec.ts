import { DetectScreenDirective } from './detect-screen.directive';

describe('DetectScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectScreenDirective();
    expect(directive).toBeTruthy();
  });
});
